# Workload Identity is enabled by google_container_cluster.workload_identity_config.
# IAM bindings between Kubernetes and Google service accounts belong to the
# consuming platform/IAM module, not this reusable GKE cluster module.
